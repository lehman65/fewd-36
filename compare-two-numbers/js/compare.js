var num1 = document.querySelector('.first-number');
var num2 = document.querySelector('.second-number');

var sign = document.querySelector('.span');
var button = document.querySelector('.compare');

//num1.addEventListener('keyup', compareNumbers);
//num2.addEventListener('keyup', compareNumbers);
button.addEventListener('click', compareNumbers);



function compareNumbers(num1, num2){
if {(num1 > num2)
	 sign = ">";
 }else if{
	(num1 < num2)
	sign = "<";

	}else {
	sign = "=";

return sign;}

var name = document.querySelector('.span')
name.textContent = sign;
console.log(sign);
}


